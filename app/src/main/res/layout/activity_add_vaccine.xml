<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F9F6F1">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="16dp"
        android:background="#FFFFFF"
        android:elevation="4dp">

        <ImageView
            android:id="@+id/backButton"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_simple_arrow_back"/>

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Add Vaccine"
            android:textColor="#000000"
            android:textSize="18sp"
            android:textStyle="bold"
            android:gravity="center"/>
    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Vaccine Name -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Vaccine Name"
                android:textStyle="bold"/>
            <EditText
                android:id="@+id/etVaccineName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="e.g. Rabies, DHPP, Bordetella"
                android:singleLine="true"
                android:background="#FFFFFF"
                android:padding="12dp"
                android:layout_marginTop="8dp"/>

            <!-- Vaccination Date -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Vaccination Date"
                android:textStyle="bold"
                android:layout_marginTop="16dp"/>
            <EditText
                android:id="@+id/vaccination_date_picker"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="Nov 12, 2023"
                android:focusable="false"
                android:background="#FFFFFF"
                android:padding="12dp"
                android:layout_marginTop="8dp"/>

            <!-- Next Due Date -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Next Due Date (Optional)"
                android:textStyle="bold"
                android:layout_marginTop="16dp"/>
            <EditText
                android:id="@+id/due_date_picker"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:focusable="false"
                android:background="#FFFFFF"
                android:padding="12dp"
                android:layout_marginTop="8dp"/>

            <!-- Status -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Status"
                android:textStyle="bold"
                android:layout_marginTop="16dp"/>
            <RadioGroup
                android:id="@+id/radioGroupStatus"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="8dp">
                <RadioButton android:id="@+id/radio_due_soon" android:text="Due Soon"/>
                <RadioButton android:id="@+id/radio_completed" android:text="Completed" android:checked="true"/>
            </RadioGroup>

            <!-- Attachments -->
            <Button
                android:id="@+id/upload_button"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Upload Certificate"
                android:background="@drawable/bg_rounded_pill_button"
                android:textColor="#000000"
                android:layout_marginTop="24dp"/>

            <!-- File Preview -->
            <LinearLayout
                android:id="@+id/file_preview_container"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:gravity="center"
                android:background="@drawable/bg_dashed_card"
                android:padding="16dp"
                android:layout_marginTop="8dp">

                <ImageView
                    android:id="@+id/file_preview_icon"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:src="@drawable/ic_document"/>

                <TextView
                    android:id="@+id/file_preview_text"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="No file selected"
                    android:layout_marginTop="8dp"/>
            </LinearLayout>

        </LinearLayout>
    </ScrollView>

    <!-- Save Button -->
    <Button
        android:id="@+id/save_vaccine_button"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="Save Vaccine"
        android:background="@drawable/bg_rounded_pill_button"
        android:textColor="#000000"
        android:layout_margin="16dp"/>

</LinearLayout>